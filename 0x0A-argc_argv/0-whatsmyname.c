#include "main.h"
#include <stdio.h>

/**
 * main - prints its name
 * @argc: print count
 * @argv: print vector
 *
 * Return: 0 always
 */
int main(int argc, char *argv[])
{
	(void)argc;
	printf("%s\n", argv[0]);

	return (0);
}
